<div class="justify-content-center d-flex flex-column">
  <h1 class="text-center"><%= @article.title %></h1>
  <div class="col-8 mx-auto">
    <div class="card text-center my-2 shadow-sm">
      <div class="card-header">
        Delian Hendardi
      </div>
      <div class="card-body">
        <p class="card-text"><%= truncate(@article.description, length: 100) %></p>
        <%= link_to 'Edit', edit_article_path(@article), class: "btn btn-info text-white" %>
        <%= link_to 'Delete', article_path(@article), data: { turbo_method: :delete, turbo_confirm: "Are you sure to delete???" }, class: "btn btn-danger" %>
      </div>
      <div class="card-footer text-muted">
        <small>Created <%= time_ago_in_words(@article.created_at) %> ago, edited <%= time_ago_in_words(@article.updated_at) %> ago</small>
      </div>
    </div>
  </div>
</div>